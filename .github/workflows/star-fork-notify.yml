name: Star and fork notifications

# DISABLED while the repository is private. A private repo produces no
# meaningful star or fork events, and the job would fail on a missing
# SLACK_WEBHOOK_URL secret. Re-enable when going public: see docs/release.md.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  notify:
    runs-on: ubuntu-latest
    if: ${{ vars.NOTIFICATIONS_ENABLED == 'true' }}

    steps:
      - name: Send Slack message
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          EVENT_NAME: ${{ github.event_name }}
          SENDER_LOGIN: ${{ github.event.sender.login }}
          SENDER_URL: ${{ github.event.sender.html_url }}
          REPO: ${{ github.repository }}
          STARS: ${{ github.event.repository.stargazers_count }}
        run: |
          jq -n \
            --arg event "$EVENT_NAME" \
            --arg login "$SENDER_LOGIN" \
            --arg url "$SENDER_URL" \
            --arg repo "$REPO" \
            --arg stars "$STARS" \
            '(if $event == "fork" then "New fork" else "New star" end) as $title
             | (if $event == "fork" then " forked " else " starred " end) as $verb
             | {text: ($title + "\n\n"
                       + "<" + $url + "|" + $login + ">" + $verb + $repo + ".\n"
                       + "Total stars: " + $stars)}' \
            > payload.json

          curl -sS --fail-with-body -X POST \
            -H 'Content-type: application/json' \
            --data @payload.json \
            "$SLACK_WEBHOOK_URL"
